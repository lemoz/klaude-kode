const status = {
  app: "Klaude Kode shell",
  mode: "bootstrap",
  message:
    "Thin TypeScript shell placeholder. Future work: render engine events from cc-engine.",
};

console.log(JSON.stringify(status, null, 2));

